spring:
  application:
    name: auth-service
  config:
    import: "optional:configserver:http://localhost:8888"
  datasource:
    url: jdbc:h2:mem:authdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8083

auth:
  jwt:
    secret: comandas-cibertec-mvp-secret-key-2026-min-32-chars
    expiration-hours: 12
