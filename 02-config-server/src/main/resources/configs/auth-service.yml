# Comandas de Restaurantes — auth-service
# JWT secret: NO definir aqui (el Config Server pisaria el JWT_SECRET del VPS).
# Usar application.yml + /etc/comandas/comandas.env (auth y gateway deben coincidir).
auth:
  jwt:
    expiration-hours: 12
